public class Classes {
    int number = 500; //Declares a class attribute and assign the value 500 to it
    public static void main (String[] args) {
        Classes classObj = new Classes(); //Instantiates a new Classes object
        System.out.println(classObj.number); //Gets the attribute 'number' from 'Classes' class and prints it.
    }
}
